version: '3.9'

services:
  app:
    build:
      context: ./P13
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    ports:
    - 8000:8000
    volumes:
     - ./P13:/P13
    env_file:
      - ./.env.dev
